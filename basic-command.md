# spec-kit-command-for-projects



# 🎯 **Basic Commands**

### 1️⃣ `/sp.init`
**Kab use karein**: Jab pehli dafa SpacKit setup kar rahe hon
**Kya karta hai**: Aapke project mein SpacKit ki settings setup karta hai
**Example**:
```bash
/sp.init
```
**Matlab**: "Chalo shuru karte hain, pehle setup karo sab kuch"

---

### 2️⃣ `/sp.chat` ya `/sp.c`
**Kab use karein**: Jab Gemini se seedha baat karni ho
**Kya karta hai**: AI se sawal jawab karne ke liye
**Example**:
```bash
/sp.chat "JavaScript mein array kya hota hai?"
```
**Matlab**: "Yaar AI se poocho kuch bhi"

---

### 3️⃣ `/sp.generate` ya `/sp.g`
**Kab use karein**: Jab code automatically banana ho
**Kya karta hai**: AI se code likhwane ke liye
**Example**:
```bash
/sp.generate "Ek login form banao React mein"
```
**Matlab**: "AI code likh ke de do mujhe"

---

### 4️⃣ `/sp.explain` ya `/sp.e`
**Kab use karein**: Jab kisi file ka code samajhna ho
**Kya karta hai**: Code ko simple language mein explain karta hai
**Example**:
```bash
/sp.explain myfile.js
```
**Matlab**: "Ye code samajh nahi aa raha, explain karo"

---

### 5️⃣ `/sp.review` ya `/sp.r`
**Kab use karein**: Jab apne code ko check karwana ho
**Kya karta hai**: Code ki mistakes aur improvements batata hai
**Example**:
```bash
/sp.review app.js
```
**Matlab**: "Mera code check karo, koi galti hai?"

---

### 6️⃣ `/sp.test` ya `/sp.t`
**Kab use karein**: Jab code ke liye tests banana ho
**Kya karta hai**: Automatically test cases generate karta hai
**Example**:
```bash
/sp.test calculator.js
```
**Matlab**: "Is code ke liye tests bana do"

---

### 7️⃣ `/sp.fix` ya `/sp.f`
**Kab use karein**: Jab code mein error ho
**Kya karta hai**: Error ko fix karne ki koshish karta hai
**Example**:
```bash
/sp.fix buggy-code.js
```
**Matlab**: "Yaar code theek kar do, chal nahi raha"

---

### 8️⃣ `/sp.doc` ya `/sp.d`
**Kab use karein**: Jab code ke liye documentation chahiye
**Kya karta hai**: Code ki documentation automatically generate karta hai
**Example**:
```bash
/sp.doc myFunction.js
```
**Matlab**: "Is code ki documentation likh do"

---

### 9️⃣ `/sp.refactor`
**Kab use karein**: Jab code ko clean aur better banana ho
**Kya karta hai**: Code ko improve karke cleaner version deta hai
**Example**:
```bash
/sp.refactor messy-code.js
```
**Matlab**: "Code ko zyada sahi tarike se likh ke do"

---

### 🔟 `/sp.translate`
**Kab use karein**: Jab code ko ek language se doosri mein convert karna ho
**Kya karta hai**: Code ko different programming language mein convert karta hai
**Example**:
```bash
/sp.translate Python script.py --to JavaScript
```
**Matlab**: "Python code ko JavaScript mein convert karo"

---

## 📝 **Real Life Example**

Maan lo aap ek website bana rahe hain:

1. **Start**: `/sp.init` - Project setup karo
2. **Code chahiye**: `/sp.generate "navbar component"` - AI se code banwao
3. **Samajh nahi aaya**: `/sp.explain navbar.js` - Code samjho
4. **Error aa rahi**: `/sp.fix navbar.js` - Error fix karo
5. **Check karna hai**: `/sp.review navbar.js` - Quality check karo
6. **Testing**: `/sp.test navbar.js` - Tests banao
7. **Documentation**: `/sp.doc navbar.js` - Docs bana lo

---

## 💡 **Pro Tips**

- Chhote commands use karein: `/sp.c` instead of `/sp.chat`
- Hamesha clear instructions dein AI ko
- File names sahi likhen

**Koi specific command ke baare mein aur detail chahiye?** Batao! 😊
